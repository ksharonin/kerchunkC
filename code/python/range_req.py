""" range_ req.py

    manually execute S3 range-get requests 
    lower-level Python code (e.g., Python AWS SDK/boto) 
    decompress and decode them into numpy arrays 
    using the information in the Kerchunk reference files 
    (byte order, dimensionality, etc.). 
"""

